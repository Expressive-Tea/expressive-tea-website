{
  "name": "expressive-tea-website",
  "type": "module",
  "private": true,
  "version": "26.8.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "check": "npm test && npm run build",
    "test": "node scripts/check-redirects.mjs && node scripts/deploy.mjs --self-test",
    "deploy": "npm run check && node scripts/deploy.mjs",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.19",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.2.8"
  }
}
